import type { ColorTokenTree } from "./index"

const radius: ColorTokenTree = {
  class: ":root",
  prefix: "--pui-radius",
  variables: {
    small: "2px",
    medium: "5px",
    large: "13px",
    full: "100%",
  },
}

export { radius }
