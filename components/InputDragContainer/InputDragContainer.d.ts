import { InputDragContainerProps } from './InputDragContainer.types';
/**
 * Wraps an `Input` prefix / suffix so its value can be scrubbed by dragging the
 * icon horizontally. Takes the props returned by `useNumericInput().getDragProps()`.
 */
export declare const InputDragContainer: ({ className, drag, children }: InputDragContainerProps) => import("preact").JSX.Element;
